# Foundry Scorecard\n\n**Score:** 80.0 / 100  \n**Confidence:** 0.84  \n**Scoring formula:** foundry-weighted-v2\n\n## Category overview\n\n| Category | Score | Weight |\n|---|---:|---:|\n| Code Quality & Architecture | 80 | 0% |\n| Maintainability & Extensibility | 84 | 0% |\n| Security & Reliability | 82 | 0% |\n| Testing & Verification | 78 | 0% |\n| Performance & Dependencies | 79 | 0% |\n| Operations | 72 | 0% |\n| Portability & Interoperability | 87 | 0% |\n| Interface & Agent Readiness | 88 | 0% |\n| Configuration | 88 | 0% |\n| Documentation & Knowledge | 84 | 0% |\n| Release & Repository Health | 58 | 0% |\n| Innovation | 80 | 0% |\n| Developer Experience | 82 | 0% |\n| Scalability | 65 | 0% |\n| License | 85 | 0% |\n\n## Strengths\n\n- **Dependency Health — 92.** Single third-party dependency: fastmcp>=3.2,<4. requirements.txt matches pyproject.toml. No unresolved dependencies (0 unresolved per analysis). 26 stdlib imports, 18 internal, 6 third-party — extremely lean dependency footprint. Version range is pinned appropriately.\n- **Agent Readiness — 92.** This is purpose-built as an agent tool. MCP server designed for LLM consumption with tool descriptions optimized for routing. The definitions tool serves as an agent onboarding endpoint. The loop-break rule and optional guard are agent-behavior-aware design features. Response contracts are machine-readable. The 'LLM is the router' principle is fundamentally agent-centric.\n- **Malleability / Extensibility — 90.** Outstanding for its domain. Adding a thinking mode requires only a JSON edit in modes.json with auto-registration — no code changes. Adding hooks requires dropping a script in a directory. The loop guard is fully configurable via env vars with sensible defaults. The modes-as-data pattern is a strong extensibility model.\n- **Architecture Coherence — 88.** Excellent separation of concerns across 8 modules with clear single responsibilities. The 'LLM is the router' principle is consistently applied — no server-side routing logic, tool descriptions carry routing. Modes-as-data decouples mode definitions from code. The no-LLM invariant is architecturally enforced (server only fills templates and appends notes).\n- **Portability — 88.** Pure Python using pathlib for cross-platform path handling. Only depends on fastmcp which is pure Python. Python 3.11+ requirement is reasonable. No OS-specific code. Should run on any platform with Python 3.11+.\n\n## Watch areas\n\n- **Contributor Concentration — 40.** Single contributor (self-audit by 'Peanutoshi'). High bus-factor risk — one person holds all knowledge. Expected for a new solo project but still a structural risk. No CONTRIBUTING.md to attract or guide new contributors.\n- **Automation Leverage — 45.** No CI pipeline, no automated testing, no pre-commit hooks, no automated releases. The hooks system provides user-side automation leverage but the repository itself has minimal automation. All quality checks (pytest, py_compile, smoke tests) are manual per the AUDIT.md.\n- **Scalability — 65.** Designed as a single-serve private instance, so large-scale concurrency is not the target. However, the in-memory _records list in store.py loads all records on startup and the guard's evaluate() calls store.export() then filters in Python — both are O(n) per operation. With heavy use and large JSONL files, this degrades. Rotation mitigates file growth but not in-memory list growth.\n- **Release Discipline — 65.** Single release (0.1.0) with version in both pyproject.toml and __init__.py. AUDIT.md documents the release with change log and verified evidence. No git tags, no CI release pipeline, no changelog file beyond AUDIT.md, no semantic versioning policy documented. Minimal but present release evidence.\n- **Performance Discipline — 70.** Not a performance-critical application — it fills templates and appends to a file. The O(n) in-memory list scans in store.export() and guard.evaluate() are acceptable for a private notepad but would not scale. No benchmarks exist, but none are needed for this archetype. The fsync-per-write is correct for durability but is the main I/O cost.\n\nDetailed machine-readable metrics, evidence, confidence, and improvement opportunities are in `scorecard.json`.